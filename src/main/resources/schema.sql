CREATE TABLE bilett (
                        id INT AUTO_INCREMENT PRIMARY KEY,
                        film VARCHAR(255) NOT NULL,
                        seter VARCHAR(255) NOT NULL,
                        fornavn VARCHAR(255) NOT NULL,
                        etternavn VARCHAR(255) NOT NULL,
                        epost VARCHAR(255) NOT NULL,
                        tellefonnummer INT NOT NULL,
                        antall INT NOT NULL


);